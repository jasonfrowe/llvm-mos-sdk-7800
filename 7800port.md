# Atari 7800 LLVM-MOS Port Status

Last updated: 2026-09-05

## Purpose

This document summarizes the work completed to implement Atari 7800 support in this LLVM-MOS SDK fork, along with current limitations and next priorities.

## Emulator Used

Primary emulator workflow used during bring-up:
- a7800 emulator (local user install)

Additional tooling used with emulator validation:
- 7800basic 7800sign for hash/signature compatibility checks on A78 outputs

Notes:
- Emulator paths are intentionally not hardcoded in project files.
- Current workflow is manual build, sign, and run.

## Target and Build Output

- Target name: atari7800
- Compiler driver: mos-atari7800-clang
- Cartridge output: A78 format
- Current ROM model: 32 KiB baseline

## Memory Layout (Current)

Hardware windows:
- RAM: 0x1800-0x27FF
- ROM: 0x8000-0xFFFF

Linker/script strategy:
- A78 header: first 128 bytes of output file
- Reset trampoline placed at high ROM (0xF000) to keep signing/hash flows compatible
- Footer and vectors reserved at top of ROM for 7800 signing compatibility

Runtime scene buffer placement:
- Scene zones are split into two banks to avoid physical shadow/conflict regions:
  - Low scene zones in .scene_zones_low at 0x1A00
  - High scene zones in .scene_zones_high at 0x2200
- Current scene zone size: 64 bytes per zone
- Default 8-line zone mode: 28 visible zones

Known sensitive regions (documented during bring-up):
- Physical shadow areas around low RAM stack/zero-page mirrors can corrupt display lists if buffers are placed incorrectly.
- Current split-zone mapping was introduced to avoid those collisions.

## What Is Implemented

Platform/runtime foundations:
- atari7800 platform integrated in mos-platform CMake
- Atari 7800 linker script with A78 header and footer/vector layout
- Startup/reset path and default interrupt stubs
- MMIO header and baseline video/input register access

Graphics and scene runtime:
- Stable NTSC DLL initialization path
- Managed scene lifecycle:
  - scene_init_160a
  - scene_begin_frame
  - scene_draw_sprite
  - scene_end_frame
- Zone activity tracking and cleanup between frames
- Palette helper support

Asset and text pipeline:
- PNG conversion pipeline for 160A assets
- Unified font import from Astro Wing assets
- Glyph-index-driven text/HUD rendering path tested in demo

Signing compatibility:
- Reset/vector layout aligned with 7800sign expectations
- Signed ROM artifacts validated in workflow

## Current Progress Snapshot

Current state is beyond initial bring-up and into gameplay-port readiness:
- Sprite rendering is stable in asset bridge demo
- HUD text can render with mapped Astro Wing glyph set
- Major linker/signing compatibility issues are resolved
- Scene memory collision issues have working mitigation

Porting status relative to Astro Wing:
- Asset ingestion path is working
- Runtime API is usable for sprite and basic HUD composition
- Work remains to make HUD/text updates cheap and deterministic every frame

## Current Issues

1. Vsync budget and frame-time pressure under heavier HUD updates
- Some text update patterns can cause visible instability (flicker, rolling artifacts, black frames).
- Current implementation needs stricter per-frame budgeting and lighter text submission paths.

2. Scene zone capacity limits
- Zone object lists are bounded by per-zone bytes.
- Over-submission in the same zone can overflow practical limits and destabilize output.

3. RAM headroom is still tight
- 6502 timing and memory margins are narrow; runtime code paths must stay minimal.
- Additional gameplay systems have not yet been layered on top of full HUD + scene load.

4. Text path maturity
- High-level scene_draw_text is functional but still needs hard guards and predictable behavior under all HUD loads.
- A pre-indexed glyph-run path (7800basic-style) is still preferred for hot frame loops.

5. Tooling integration gaps
- Build-integrated signing and emulator launch are not yet fully automated in-repo.

## Immediate Priorities

1. ~~Introduce a lean pre-indexed glyph-run API for HUD paths~~ (Done 2026-09-05)
- Added `atari7800_build_glyph_run` (one-time resolution) + `atari7800_scene_draw_glyph_run` (hot-loop blit) to `mos-platform/atari7800/atari7800.{h,c}`.
- `astrowing.c`'s HUD text now builds its glyph run once at startup and redraws it every frame with no string parsing in the hot loop.
- Confirmed in the emulator: HUD ("SHLD:100 L:3") still renders correctly.

2. ~~Add hard guards for zone/object budget~~ (Done 2026-09-05)
- The per-zone RAM overflow guard already existed (`atari7800_maria_plot_sprite_zone5` bounds-checked against `zone_size`); what was missing was an *explicit* status, since it only returned a boolean nobody checked.
- Added `ATARI7800_OK` / `ATARI7800_ERR_INVALID` / `ATARI7800_ERR_BUDGET_FULL` status codes across every sprite/text/glyph-run draw call (note: flips the old convention — `ATARI7800_OK` is 0, so any code still checking `if (!fn())` needs updating; nothing in this repo did).
- `astrowing.c` now checks these via a small `note_status()` helper as a worked example, rather than silently ignoring them.
- Not done: clipping/wrapping behavior on overflow (current behavior is still drop-on-full, now just traceably) and a pre-flight capacity query — deferred, no concrete need for either yet.

3. ~~Keep static HUD residency and update only dirty elements~~ (Done 2026-09-05)
- Added `atari7800_scene_set_zone_static` to `mos-platform/atari7800/atari7800.{h,c}`: pins a zone so `begin_frame`/`end_frame` skip resetting/wiping it entirely, instead of unconditionally re-validating every zone every frame.
- Tracked as a packed bitmask (4 bytes for 28 zones), not one byte per zone, given tight RAM.
- `astrowing.c`'s HUD text (static in that demo) now draws once at startup and pins its zone; the per-frame loop no longer touches it at all. Gameplay zones (ship, stars) are untouched by this and remain fully dynamic every frame, as before.
- Not yet verified visually in the emulator — needs a hands-on check that the HUD still renders correctly with this path.

4. ~~Add lightweight timing/perf instrumentation~~ (Done 2026-09-05)
- No new SDK API needed -- `ATARI7800_BACKGRND` was already public. Added the standard 6502-homebrew "background flash" technique to `astrowing.c`, guarded by the `ATARI7800_DEBUG_FRAME_BUDGET` compile-time macro (new CMake option `ATARI7800_ASTROWING_DEBUG_FRAME_BUDGET`, default OFF), split into 4 differently-hued stages so a single screenshot shows which part of the frame loop is expensive.
- **This immediately found a real bug**: emulator testing showed 25-30% of the visible screen corrupted every frame (already bad at idle), and the per-stage breakdown pinned it on `atari7800_scene_begin_frame` costing more than the sprite draw calls it gates. Root cause: priority #3's static-zone bitmask needed a runtime-variable bit shift (`1u << (n & 7u)`) to test/set an arbitrary bit, which has no single-instruction form on 6502 -- fixed by switching to one byte per zone (see priority #3 above). Verified via the same marker: total per-frame overrun dropped from 61 to 38 scanlines (~27% to ~17% of the visible screen).
- **Not fully fixed by micro-optimization alone**: 38 scanlines was still a real overrun for a demo this small. Investigating further led to comparing the whole object-lifecycle model against 7800basic's actual runtime (see priority #5 below), rather than continuing to patch the append-and-diff model.

5. ~~Adopt persistent sprite objects (7800basic-style)~~ (Done 2026-09-05)
- 7800basic's runtime (`~/Software/Atari7800/7800basic/includes/updateobjects.asm`, `zonememory.asm`) doesn't re-derive zone membership and re-append a fresh object header every frame the way our `atari7800_scene_draw_sprite` + `begin_frame`/`end_frame` did. It assigns each sprite a persistent slot and patches its existing header in place frame-to-frame. Also confirmed 7800basic's default zone height is 16, not 8 (matching the half-built `ATARI7800_ZONE_HEIGHT=16` option already in this codebase, not yet adopted). Checked cc65's Atari 7800 target too -- it's conio/text-console only, nothing relevant to sprites.
- Added `atari7800_scene_object_t` / `atari7800_scene_sprite` / `atari7800_scene_hide_sprite` to `mos-platform/atari7800/atari7800.{h,c}`, adapted from 7800basic's model with one correction: slots are local to whichever zone an object currently occupies (not global across every zone) since our display lists are compact-plus-terminator, unlike 7800basic's always-fully-populated zones -- a global slot would leave stale bytes in the gap between non-adjacent occupants sharing a zone. Moving to a new zone parks the old slot off-screen rather than reclaiming it, so a zone's slot usage only grows even if live occupancy shrinks -- a known, narrow limitation for scenes with heavy zone-crossing churn, not hit by anything in `astrowing.c` today.
- `astrowing.c` migrated all 4 stars and both ship-half sprites to persistent objects and dropped its per-frame `begin_frame`/`end_frame` calls entirely. HUD text is untouched (already handled by static zone residency, priority #3).
- Re-measured with the same frame-budget marker: total overrun dropped from 38 scanlines to roughly 15 (~6% of the visible screen, down from ~27% where this investigation started). Visually confirmed correct in the emulator (ship, stars including hide/show at the y=16 HUD boundary, HUD text).
- Resolved the hue-labeling confusion from the initial measurement: disassembly plus a targeted A/B test (rebuilding with the game-logic marker write removed) confirmed game logic contributes zero visible overrun (fully absorbed in vblank) and the **entire remaining ~15-scanline band is the star + ship persistent-object draw calls themselves** (6 sprite plots/frame). The earlier "0x8f = green" color attribution from the very first round of measurement was simply wrong and never independently re-verified; it's actually light blue. Not chased further tonight -- 15 scanlines (~6% of the visible screen) for 6 sprite draws is a reasonable baseline to build more gameplay on and re-measure once there's more on screen, rather than over-optimizing this toy scene now. The `ATARI7800_ZONE_HEIGHT=16` option remains an available lever if a future scene needs more headroom.

6. Enemy sprite (in progress, 2026-09-05)
- Checked the reference 7800basic source (`~/Software/Atari7800/Astrowing/src/astrowing.bas`) before building anything new: its regular enemy pool reuses the player's own non-rotating ship shape (`fighter.png`) recolored via the "Enemy" palette (`P3C1-3 = $B4,$46,$1C`), not new art. That exact asset (`fighter_sprite` in `examples/atari7800/assets/fighter.sprite.h`, already `palette=3`) has been sitting in this repo unused since the early asset-bridge-demo work, and the "Enemy" palette was already reserved in `astrowing.c` matching those exact values -- so this needed zero new asset conversion.
- Added a single persistent-object enemy (`enemy_obj`) with throttled horizontal drift bouncing between x=16/144, matching astrowing.bas's frame-masked movement technique (`frame & enemy_move_mask`) simplified to one enemy. Fixed Y (32, zone-aligned): `fighter_sprite_data` has no fine-Y padding like `star_sprite_data`/`spaceship_data` does, so it can't safely use the `draw_sprite_fine` page-shift trick without extending the asset first.
- Visually confirmed in the emulator: enemy renders in its own color, distinct from the player ship, drifting as expected.
- **Found a bigger issue while checking frame budget**: adding this one draw call jumped the measured overrun from ~15 to ~51 scanlines, but the compiled code for the call is only 37 bytes -- nowhere near enough to explain +36 scanlines through normal execution. This supports the MARIA DMA-cycle-stealing theory from earlier: code executing *after* the vblank boundary is contending with active-display DMA, so it costs disproportionately more real time than the same code during vblank, and the effect likely compounds as more objects add more DMA traffic.

7. ~~NMI-driven display sync~~ (Done 2026-09-05, doesn't help this scene)
- Adapted 7800basic's MARIA-fired-NMI kernel synchronization (`~/Software/Atari7800/7800basic/includes/std_routines.asm`'s `visibleover` toggle) into `atari7800_scene_enable_nmi_sync` / `atari7800_scene_wait_active_start` / `atari7800_scene_wait_display_safe` in `mos-platform/atari7800/atari7800.{h,c}`. First-ever use of the platform's NMI vector; verified correct via disassembly (proper register save/restore, ends in `RTI`) and confirmed no regression in `palette-demo`/`displaylist-demo`/`asset-bridge-demo` (none of which opt in, so NMI never fires for them).
- Hit and fixed a real design bug along the way: the first version's `wait_display_safe()` didn't actually let logic overlap active-display time (a single level-triggered wait called after logic just idles away the safe window before ever reaching busy time). Needed splitting into `wait_active_start()` (before logic) / `wait_display_safe()` (before draws) to get real overlap, matching how 7800basic's `drawscreen` waits bracket the *whole* loop body from the outside, not something sandwiched in the middle.
- **Result: doesn't help `astrowing.c`.** Game logic was already measured as effectively free (sub-scanline, absorbed in blanking) hours earlier -- the entire benefit of this mechanism is letting logic overlap active-display DMA time, and there's no benefit to overlapping work that was never expensive. The actual bottleneck is draw-call cost itself, which no amount of loop restructuring can reduce. Kept in the SDK as validated infrastructure for a future logic-heavier scene; `astrowing.c` stays on it since it's correct and no worse than the old polling model, just not the fix this scene needed.

8. ~~Switch astrowing.c to 16-line zones~~ (Done 2026-09-05)
- astrowing.bas (the reference source) uses `set zoneheight 16`; confirmed 7800basic's own default is 16 too. Enabled via `#define ATARI7800_ZONE_HEIGHT 16` before including the platform header (already-scaffolded, previously never actually exercised by anything).
- **Found and fixed a real, previously-latent platform bug in the process**: `ATARI7800_ZONE_OFFSET` (the MARIA DLL "lines-1" encoding used both in the static blank-DLL builder and every dynamic zone-link call) was defined as `0u` for 16-line zones instead of `15u` -- MARIA was being told every zone was 1 scanline tall instead of 16, derailing the entire display list's timing and corrupting most of the visible screen. This has presumably been silently wrong since 16-line zones were first scaffolded; nothing ever exercised that code path before tonight. Fixed in `mos-platform/atari7800/atari7800.h`; the static blank-builder in `atari7800.c` now uses the named constant instead of a second hardcoded (and, for the 16-line branch, wrong) literal.
- Re-measured with the same frame-budget marker: overrun dropped from 66 to 52 scanlines (~29% reduction) on top of the NMI-synced loop. 52 scanlines is still a real overrun -- not chased further tonight.

**Correction (same night, caught by hands-on testing, not automated screenshots):** the first version of this also tried simplifying the ship from two manually-split 8-line objects to one `atari7800_scene_object_t` drawing `spaceship_frames[angle]` directly as a single 16-line object, reasoning that since it's natively 16 lines tall it should fit one 16-line zone exactly. **This was wrong and shipped broken** -- only the bottom half rendered. Confirmed by a controlled A/B test (same pinned rotation angle, single-object vs. two-stacked-objects, side by side in one screenshot): a single MARIA Direct Mode object empirically renders only ~8 scanlines *regardless of the containing zone's declared height*. 16-line zones let a *zone* span 16 scanlines and let *multiple* objects share that zone, but an individual object still needs splitting to cover more than half of it -- matching 7800basic's own docs ("to use taller sprites, simply define more sprites and position one above the other") and its `EnableObject`/`SetObjectY` macros, which use a *different* bit (`%00010000` for zoneheight=16 vs `%00001000` for zoneheight=8) to select which half of a 16-line zone an 8-line object occupies -- i.e. 7800basic never draws a genuine single 16-line-tall object either.
- Reverted the ship to two stacked 8-line objects (`ship_top_obj`/`ship_bottom_obj`, at y=80/88) -- the only change from the original 8-line-zone code is that both objects now land in the *same* 16-line zone instead of two separate zones.
- The same root cause explained a second bug: stars scrolling in and out of view in 8-pixel bands. `draw_sprite_fine`'s fine-Y shift (`y & 7`) can only reach the bottom half of whatever 16-line zone a star is in, since it only has one reference point into `star_sprite_data`. Fixed by giving the star asset a second reference (`star_sprite_upper`, an 8-page-shifted view into an extended `star_sprite_data`) and selecting between the two at the call site based on `(y & 15) < 8` -- same half-select principle as the ship, generalized to a single-pixel sprite.
- Both fixes verified in the emulator (ship renders complete at a pinned rotation angle; stars stay visible across a longer, more-scrolled run).

9. ~~Fix persistent-object zone corruption~~ (Done 2026-09-06)
- **Process note first**: the user re-tested the "ship half-rendered / stars gap" fix above (commit `73559d9`) via `build/dev-bootstrap/examples/atari7800/astrowing.a78` and still saw the old bugs. Root cause was mine, not the code: that path is the OUTER, installed copy from a nested ExternalProject build (`build/dev-bootstrap/examples/atari7800/build/atari7800/astrowing.a78` is the real, freshly-compiled artifact); several verification rounds had rebuilt only the nested copy (`ninja astrowing.a78` from inside the nested build dir) without re-running the install step, so the outer file the user actually tested was stale. Fix: always rebuild via `ninja atari7800-examples` from `build/dev-bootstrap` (the full top-level target, which runs the install step), never the nested shortcut.
- With a properly fresh build in hand, the user hands-on-tested again (real keyboard input, not headless) and found two *further*, genuine bugs, both in the persistent-object model (`atari7800_scene_sprite`) added in priority #5:
  - **Stars vanish sharing a row with the ship/enemy.** `atari7800_maria_plot_sprite_zone5` always writes 7 bytes per object (5 header bytes + 2 "terminator" bytes that double as the *next* slot's first 2 header bytes). That's correct when a zone's whole object list is rebuilt fresh in one pass (the old `atari7800_scene_draw_sprite` model), but `atari7800_scene_sprite`'s "patch in place" path also went through that same 7-byte write on every per-frame redraw of an *already-placed* object -- silently zeroing the address/mode bytes of whatever occupies the next slot in that zone. Since the ship/enemy claim their zone slot once, early, and hold it indefinitely, they reliably end up at a *lower* slot number than any star that later drifts into their zone; since the game loop draws ship/enemy *after* stars, their redraw stomped the star's header every single frame.
  - **Stars vanish permanently after scrolling a bit, except near the top.** Persistent-object slots are parked (not reclaimed) on a zone change (see priority #5's own noted limitation) -- so a star re-entering a zone it had already visited before burns a brand-new slot every time, never reusing one. A 64-byte zone holds ~12 slots; four stars continuously crossing zone boundaries while scrolling exhaust a commonly-revisited zone's budget within a couple hundred frames, at which point `atari7800_scene_sprite` returns `ATARI7800_ERR_BUDGET_FULL` -- and because the object's `zone_index` bookkeeping isn't rolled back on failure (its *old* slot was already parked off-screen earlier in the same call), it stays permanently parked and keeps failing the same way forever. Zones visited less often (near the top of the scroll range) hadn't yet exhausted their budget, matching the reported "only visible near the top" symptom exactly.
  - Both are inherent to "patch in place forever" once more than one object can share a zone -- not fixable by tuning constants. Fixed by moving stars/ship/enemy in `astrowing.c` back to `atari7800_scene_draw_sprite` + `begin_frame`/`end_frame` (priority #4's model, still fully correct and already in the codebase): every zone's object list is re-derived from scratch each frame in that frame's own draw order, so there's no cross-frame slot staleness and no unbounded growth. The persistent-object API stays in the platform for genuinely static content (the HUD zone still uses it safely, since it never crosses a zone boundary) -- its doc comment in `atari7800.h` now spells out the zone-crossing-churn caveat explicitly.
  - Also fixed the underlying corruption at the platform level regardless: added `atari7800_maria_patch_sprite_zone5` / `atari7800_maria_patch_sprite_asset_zone5` (5-byte-only rewrite, doesn't touch the next slot) and switched `atari7800_scene_sprite`'s patch-in-place branch to use it. This doesn't fix the slot-growth issue (still real, still documented), but means a future caller of the persistent-object API for a *zone-stable* multi-object zone won't hit the header-clobbering bug astrowing.c did.
  - Verified with a temporary, reverted test-only change forcing continuous thrust (real keyboard testing wasn't available headless) to actually exercise scrolling: 6 snapshots from 3-20 seconds of continuous forced scrolling all show all stars still rendering as clean single-pixel dots at varying positions (never collapsing toward only the top), ship rendering complete, no corruption artifacts. The change was reverted before committing; `git diff --stat` after revert shows only the intended 3 files (`astrowing.c`, `atari7800.c`, `atari7800.h`).

10. ~~Fix enemy disappearing when a star shares its row~~ (Done 2026-09-06)
- Immediately after the fix above, the user hands-on-tested again and found a *third* bug (not yet present in their earlier reports): "if a star and the enemy sprite share the same row, then the enemy sprite disappears."
- Repro'd with a temporary, reverted test hack forcing a star to the enemy's exact (x, y): confirmed via pixel-level screenshot analysis that with the two objects at the exact same position, *neither* renders at all (not even the enemy's own multi-color sprite art, which briefly got misdiagnosed mid-investigation as a "corrupted overlap" before double-checking a clean baseline showed the same pink/green/yellow pattern is just the fighter sprite's actual art). Narrowed the trigger condition with further controlled tests: two Direct Mode objects needing pixel data on the *same 8-line scanline band* (confirmed via this session's own earlier finding that objects render in ~8-line units regardless of zone height) *and* overlapping in X both fail to render; separating them by X, or by scanline band (e.g. top vs. bottom half of a 16-line zone), renders both cleanly every time.
- This is a genuine MARIA Direct Mode hardware characteristic, not a software bug: Direct Mode has no per-pixel object-priority resolution, so two objects contending for the same physical scanlines and pixel columns produce undefined (here: both blanked) results. Not fixable underneath callers in the platform -- the fix has to be at the draw-order/game-logic level, avoiding the collision before it happens.
- Added a small `bands_collide()` helper to `astrowing.c` (approximate AABB check: same `y >> 3` 8-line band, plus a generous ±10px X window) and used it to skip drawing a star for one frame if it would land on the same band and overlap in X with the enemy or either ship half. Verified: forcing exact overlap with the guard active renders the enemy's normal complete sprite (star silently skipped that frame); disabling the guard with the same exact overlap reproduces total disappearance again, confirming the guard is both necessary and sufficient for this case. A 20-second stress run (continuous forced thrust, same technique as priority #9) shows the enemy rendering correctly in every sampled frame.
- Not generalized into the platform: this is scoped to astrowing.c's specific objects (stars vs. enemy/ship). Any future object added to this scene that can share a scanline band with another (bullets, asteroids) will need the same kind of avoidance check -- worth revisiting as a small reusable helper if/when several object types need mutual collision-band awareness, but not before there's a second real caller.

**Correction (2026-09-06, same investigation continued): priority #10's diagnosis was wrong.** The user pushed back after re-testing: the enemy still disappeared sharing a row with a star regardless of X distance, contradicting the "X-overlap" theory, and flagged that the whole display-list-building approach needed rethinking -- pointing at the 7800 Software Guide (7800.8bitdev.org) and the 7800basic compiler source/docs as references. The guide's own account of MARIA Direct Mode headers (4/5-byte layout, width-field formula, terminator convention) matched this SDK's implementation almost exactly, which didn't fit "hardware can't handle two objects on a line" either -- real 7800 games routinely have far more than two objects per scanline.

## 11. ~~Fix ATARI7800_ZONE_HEIGHT not actually reaching the compiled scene code~~ (Done 2026-09-06)

**The actual root cause, months in the making**: `mos-platform/atari7800/atari7800.c` is compiled exactly *once*, into a prebuilt static library (`libc.a`), as part of SDK bootstrap -- before any example (including `astrowing.c`) is ever compiled. That compile uses the header's *default* `ATARI7800_ZONE_HEIGHT` (8), because nothing at that point can know astrowing.c wants 16. `astrowing.c`'s `#define ATARI7800_ZONE_HEIGHT 16` only ever affected its own translation unit -- it had zero effect on any function inside `atari7800.c` (`atari7800_scene_draw_sprite`, `atari7800_scene_sprite`, `atari7800_maria_build_blank_ntsc`, the whole zone/DLL machinery), which had been silently running on 8-line-zone geometry (`ATARI7800_ZONE_SHIFT=3`, 28 zones, 33 DLL entries) this entire time, while `astrowing.c`'s own reasoning (its half-select logic, its now-deleted `bands_collide` guard, the whole "ship needs two stacked 8-line objects" premise from priority #8's correction) assumed 16-line geometry. Two halves of the same program disagreeing about where zone boundaries are -- confirmed directly by dumping the linked binary's symbols: `atari7800_scene_display_list` was 99 bytes (33 entries) and `atari7800_scene_zones_low` was 896 bytes, both exactly the 8-line configuration, in a ROM that `#define`s `ATARI7800_ZONE_HEIGHT 16`.

This retroactively explains nearly everything chased across priorities #5 through #10 tonight as symptoms of this one bug, not real MARIA hardware behavior:
- The "single Direct Mode object only renders ~8 scanlines regardless of the containing zone's declared height" finding (priority #8) -- there is no such cap; the library was always building 8-line zones.
- The ship needing two stacked 8-line objects, and the star needing two half-references -- unnecessary once zones are genuinely 16 lines.
- Priority #10's "MARIA Direct Mode has no per-pixel object priority, two objects on a scanline band corrupt" theory -- also wrong; the `bands_collide` guard happened to mask the real bug in some test configurations by accident, not because scanline-sharing is actually unsafe.

**The fix** (user chose this option explicitly over just hardcoding one zone height platform-wide): moved every zone-height-dependent piece of `atari7800.c` -- the zone/DLL static state and every function that touches `ATARI7800_ZONE_SHIFT`/`ATARI7800_SCENE_VISIBLE_ZONES`/the zone buffers (`atari7800_maria_build_blank_ntsc`, `atari7800_scene_init_160a`, `atari7800_scene_begin_frame`/`end_frame`, `atari7800_scene_set_zone_static`, `atari7800_scene_draw_sprite`, `atari7800_scene_sprite`/`hide_sprite`, `atari7800_scene_draw_text`/`draw_glyph_run`, the NMI machinery) -- into `static`/`static inline` definitions directly in `atari7800.h`, so they compile fresh into each program's own translation unit and genuinely honor that program's own `#define ATARI7800_ZONE_HEIGHT`. `atari7800.c` now contains only the handful of truly zone-height-independent functions (vblank/palette/low-level 5-byte-header plot/patch functions, glyph-run building), and defines `ATARI7800_NO_SCENE_IMPL` before including the header so it doesn't also pull in a second copy of the zone-height-dependent block (which would otherwise collide with every program's own copy, in particular the real, non-static `nmi` symbol the linker's weak-vector-override needs -- hit and fixed this exact duplicate-symbol error during the refactor). Documented the resulting constraint clearly in the header: use the scene API from exactly one `.c` file per program (already true of every example in this SDK).

**Verified thoroughly**:
- Symbol dump after rebuild: `astrowing.a78` now genuinely has 57-byte (19-entry) `atari7800_scene_display_list` and 448-byte `atari7800_scene_zones_low` -- real 16-line geometry, for the first time ever. `asset-bridge-demo.a78` (no `#define`, default height) still correctly gets the 8-line, 99-byte/896-byte configuration. `palette-demo.a78` (doesn't use the scene API at all) has zero bytes of scene state -- dead code elimination works correctly per-program.
- All four existing demos (`palette-demo`, `displaylist-demo`, `asset-bridge-demo`, `astrowing`) rebuilt and visually confirmed correct in the emulator -- no regressions from the refactor.
- The original bug (enemy disappearing sharing a row with a star): reproduced with a temporary forced-exact-overlap test *with the `bands_collide` guard disabled* -- both objects rendered correctly, fully confirming the guard was never actually necessary once the real bug was fixed. Removed `bands_collide` entirely from `astrowing.c`.
- Bonus: since the ship asset (`spaceship_frames[]`) already has genuine 16 real lines of pixel data (`SPACESHIP_HEIGHT_LINES`), a single real 16-line object now renders it completely and correctly -- removed the two-stacked-8-line-object workaround from priority #8's correction, simplifying the ship draw call back to one `draw_sprite_fine` call. The star's two-reference/half-select approach was left as-is (proven correct, not a correctness risk) but is very likely also unnecessary now -- noted as a follow-up, not re-verified tonight.
- 20-second stress run (continuous forced thrust, same technique as priority #9): ship, enemy, and stars all render correctly and consistently throughout, including frames where a star sits directly next to the enemy's row.

## 12. ~~Fix garbage/duplicate pixels from assets sized for the old 8-line render window~~ (Done 2026-09-06)

Immediately after priority #11 landed, the user hands-on-tested and found three new artifacts: a solid yellow line above the enemy sprite, every star plotted twice (~8 lines apart), and stray dashes above the HUD text. All three turned out to be the same underlying cause, exposed (not created) by priority #11's fix -- assets that were sized/designed assuming a Direct Mode object's render window only ever covered ~8 lines (the old, broken behavior), now that objects genuinely render their *zone's* full declared height (16 lines) regardless of the object's own content:

- **`fighter_sprite_data`** (enemy) and **`hud_font_data`** (HUD glyphs), both in `examples/atari7800/assets/fighter.sprite.h`, were declared with an *implicit* array size (`uint8_t foo[] = { [1792] = ..., ... }`), which the compiler sizes to just past the highest explicit index -- covering only their real 8 pages (2048 bytes) of pixel data. A 16-line object now reads 16 pages regardless, so the extra 8 pages read whatever ROM bytes the linker happened to place immediately after each array (unrelated code or data) instead of blank. Given the "vertically flipped for MARIA scanline countdown" layout both assets already used (noted in their own comments), this garbage lands at the *top* of the object's zone rather than the bottom, matching exactly what was reported: a stray line above the enemy, stray dots above (not below or overlapping) the HUD text. Fixed by explicitly sizing both arrays to 4096 bytes (16 pages) so the extra 8 pages are explicit zero instead of adjacent ROM garbage -- harmless in an 8-line zone too (e.g. `asset-bridge-demo`, which doesn't override `ATARI7800_ZONE_HEIGHT`), since only the first 8 pages are ever read there.
- **`star_sprite_data`** (`astrowing.c`) hit a different failure mode from the same cause. Its two-reference design (`star_sprite` / `star_sprite_upper`, one pixel at page 7 and one at page 15) was built so that each reference's *effective* 8-line render window would isolate one pixel from the other -- back when the window really was 8 pages regardless of which reference was used. With windows genuinely spanning 16 pages now, *any* fine-Y shift's window (`[shift, shift+15]` for `shift` in 0-7) includes both page 7 and page 15 simultaneously, so both pixels render at once, 8 lines apart -- doubling every star. Fixed by collapsing to a single reference (pixel at page 15 only, matching the same page-`(N-1)` placement the old proven-correct 8-line formula used, generalized to `N=16`), extending the fine-Y shift range from `y&7` to `y&15` in `draw_sprite_fine`, and explicitly sizing the array to 31 pages (7936 bytes) so the full `shift` range (0-15) never reads past explicit padding.
- Verified via pixel-level screenshot analysis (not just eyeballing): the yellow line above the enemy, the stray HUD dashes, and the duplicate star positions are all gone; a repeat of the 20-second continuous-forced-scroll stress test shows clean single-instance stars, a clean enemy, and a clean HUD throughout. All four demos re-confirmed visually correct, including `asset-bridge-demo` (which shares the now-repadded `fighter_sprite_data`/`hud_font_data` at the *default* 8-line zone height).

**Takeaway for future assets**: any sprite or font asset drawn into a 16-line zone needs to actually declare 16 pages of data (explicit trailing/padding zero bytes), even if the real art is shorter -- an object's render window is dictated by the *zone's* declared height, not the asset's own `height_lines` field (which nothing in this code path even reads). This wasn't visible before priority #11 because the platform was secretly always using 8-line zones; it's a real, permanent constraint now that zones are genuinely 16 lines.

## References in This Repo

- mos-platform/atari7800/link.ld
- mos-platform/atari7800/atari7800.h
- mos-platform/atari7800/atari7800.c
- docs/atari7800-plan.md
- docs/atari7800-api.md
- docs/atari7800-astrowing-intake.md
- docs/atari7800-scene-api-roadmap.md
- examples/atari7800/asset-bridge-demo.c

## 13. Title screen + POKEY: first astrowing.bas content port, as a shakedown (Done 2026-09-06)

With the platform's Direct Mode scene API verified correct against real 16-line-zone geometry (priorities #11-#12), started porting real `astrowing.bas` content for the first time: the title screen and its POKEY theme song, picked deliberately as a shakedown of the graphics/memory-management path across every visible zone at once (rather than diving straight into "levels," which needs a collision/bullet/spawn system this port doesn't have yet).

- **`plotbanner` needed no new rendering mode.** Confirmed via the 7800basic guide: *"to accomplish plotting a banner 'N' zones tall, 7800basic actually plots 'N' sprites"* -- it reuses the exact same Direct Mode object mechanism `atari7800_scene_draw_sprite` already implements.
- **Used the real reference assets**, not placeholders: `title_screen_conv.png` (160x96, 4-color, from `~/Software/Atari7800/Astrowing/src/graphics/`) and `Song_01_30hz.bin` (the actual title theme, confirmed via `astrowing.bas`'s own `PlayMusic` comment "Title, Level 2, Level 5" and its title-screen branch).
- **New converter**: `utils/png_banner_to_zones_header.py` slices a tall indexed PNG into per-zone MARIA-strided objects. Two real constraints surfaced and got fixed during development, not anticipated in the plan:
  - MARIA's 5-bit width field caps a single Direct Mode object at 31 bytes (124px in 160A) -- the 160px-wide title needed splitting into 2 side-by-side objects per zone, not 1.
  - The first version gave each of the resulting 12 (zone, chunk) slices its own fully-padded 16-page (4096-byte) block -- 48KB total, blowing the 32KB ROM. Fixed by applying the same technique `pack_sprites_to_strided.py` already uses for multiple animation frames sharing one page block (each slice at a different byte offset within every page) to these zone/chunk slices instead: one shared 16-page (4096-byte) block for the whole banner.
  - The exact packing direction (row 0 of a zone-aligned slice goes at the *highest* page, not the lowest) was confirmed empirically before writing the script, via a temporary two-marker test comparing a page-0 pixel against a page-15 pixel at the same zone-aligned Y -- not assumed from the fighter/font assets' analogous convention.
- **POKEY**: added `mos-platform/atari7800/atari7800_pokey.h` (register defs + `atari7800_pokey_step`, a direct C reimplementation, per the user's choice, of `PlayMusic`'s byte-stream interpreter -- (register, value) pairs, 0xFF = end of frame, 0xFE = loop). Music data embedded via the existing `utils/import_binary_as_c_header.sh`, unchanged.
- **POKEY hardware declaration turned into a real, load-bearing discovery.** The `.a78` header must declare POKEY or nothing routes `$0450` to a chip at all -- confirmed by checking this SDK's actual test emulator's device tree before and after the header change (no `pokey` device present without it, one appears with it). Two further findings, neither guessed at:
  - This specific test emulator (a 7800-devtools/a7800 fork) only reads the **legacy v3 cart-type byte** (byte 54, bit 6) for POKEY detection -- not the v4 "audio" field a header declaring version 4 "should" prefer per the A78 header spec. Found by directly inspecting `a78_slot.cpp` in the emulator's own source after setting the v4 field alone produced no POKEY device.
  - **Setting that v3 bit broke rendering outright** (a fully black screen) -- it switches the emulator's cart-slot device to a different C++ class (`a78_p450_t0`) that doesn't correctly support this platform's plain 32KB-flat-ROM layout. Isolated by toggling just that one header byte on an already-built ROM and re-running: black with the bit set, correct the instant it's cleared. Not investigated further -- correct rendering matters more than audio in one emulator fork. Resolution: `__cart_pokey_flags` still sets the v4 field correctly (real hardware/v4-aware tools), but deliberately does *not* touch the v3 bit; audio is untested for actual sound as a result (see below).
  - Also hit and fixed a rebuild-staleness bug matching this session's now-familiar pattern: changing `link.ld` alone didn't trigger ninja to relink already-compiled `.a78` outputs; had to `rm` the nested build's `.a78`/`.a78.elf` files directly to force it. Worth remembering for any future `link.ld`-only change.
- **Honest status on audio**: the header declares POKEY correctly (verified: device tree gains a real `pokey` device), and the compiled 6502 code correctly computes and writes to `$0450+register` (verified via disassembly trace: indirect-indexed store to a runtime-computed pointer that resolves to exactly `0x0450+reg`). What's *not* confirmed is whether those writes visibly changed the device's live register state over time -- Lua state introspection (`pokey.state`) showed no change across ~3 seconds of runtime, but this is suspected to be a limitation of that introspection path in this MAME build rather than a real bug, given the header/device-instantiation and disassembly are both independently correct. Cannot listen to audio in this headless environment. **Needs a hands-on check with real sound output** to close this out.
- Verified: all 5 examples (`palette-demo`, `displaylist-demo`, `asset-bridge-demo`, `astrowing`, `title-demo`) screenshot-confirmed correct after every change in this section, including after the v3-bit regression was found and reverted.

## 14. Title screen fidelity pass: real colors, position, palette cycling, missing text (Done 2026-09-06)

User hands-on-tested `title-demo` against the actual 7800basic-compiled reference ROM (`~/Software/Atari7800/Astrowing/build/output/astrowing.a78`, confirmed to play audible POKEY music in the emulator -- see below) and found four gaps: no palette animation, wrong banner position (top instead of center), missing text, and (expected/already known) no sound. Root cause for the first three: the port used guessed/placeholder values instead of the reference's actual runtime behavior. Fixed by reading `astrowing.bas`'s `title_loop` directly rather than continuing to guess:

- **Real palette bytes, not PNG-derived guesses.** The title banner's PNG has its own embedded indexed-color palette (used only for the *converter* to read pixel indices), but the actual in-game colors come from explicit `P7C1/C2/C3 = $C8/$46/$1C` in the BASIC source ("Title Screen (Vibrant)") -- completely different from the yellow/orange/white this session first extracted from the PNG's own palette. Also picked up the real values for palettes 1 (`$C2/$C6/$CA`, "Player Bullets/Green", used for the VERSION/DIFFICULTY text) and confirmed 5 ("Spaceship", already known from `astrowing.c`).
- **Live palette-7 hue-cycle animation**, transcribed directly from `astrowing.bas:349-365` (`temp_acc = frame/4`, then three `(base + temp_acc) & 15` hue rotations at fixed luminance per channel) -- runs every frame as a handful of palette-register writes; the banner's zones/objects stay pinned static and are never redrawn. Confirmed animating correctly via a 6-frame screenshot sweep (clean rotation through blue/purple/green/pink, matching the reference's own look).
- **Banner repositioned** from y=0 to y=64 (zone-aligned; the reference's actual y=46 needs cross-zone fine-Y splitting, not implemented -- see priority #13's note). y=64 both centers the 96-line banner exactly within this platform's 224-line total zone space and visually approximates the reference's own proportions.
- **Added the missing VERSION/DIFFICULTY/PRO/date/score text**, transcribed from `astrowing.bas:341-350`'s `plotchars`/`plotvalue` calls. Hit a real, previously-undiscovered platform limit while doing this: a single-zone glyph run is capped at 12 characters by the zone's 64-byte object budget (5-byte header stride; confirmed hands-on -- both of the reference's combined-string rows, 13 and 15 non-space glyphs, silently truncated exactly at the 12th glyph, matching the math exactly). Fixed by splitting "DIFFICULTY"/"PRO" and "VERSION"/"20260126" onto four separate zone rows instead of the reference's two compact ones -- preserves exact wording at the cost of the reference's tighter layout. Worth remembering for any future multi-word single-line HUD text.
- **POKEY audio status, meaningfully clarified**: the reference ROM's header has the *same* v3 "POKEY @ $450" bit this session found breaks this platform's rendering when set (confirmed via a hex dump: byte 54 = `0x40`, identical bit) -- but the reference ROM is 48KB (`0x0000C000` in its header's ROM-size field) versus this platform's fixed 32KB. Likely explanation, not yet confirmed: the emulator's `a78_p450_t0` cart-slot class may assume a ROM layout only a larger/bankswitched image satisfies, which this platform's flat 32KB mapping doesn't.
  - **Ran the concrete next step this same session**: pointed the exact same Lua `pokey.state` introspection script at the reference ROM. Result: register values visibly change over time (e.g. `AUDF1` goes from the POKEY reset default straight to `243` a couple seconds in, then keeps changing every music tick) -- and `243` is exactly `Song_01_30hz.bin`'s first byte pair value (`0xf3`), the same file this port's `title-demo` embeds. This proves the introspection method itself is reliable in this MAME build, which reframes the earlier "inconclusive" result: that check was run on a build that *also* had the v3 bit set, at a point before the rendering-breaking regression (priority #13) was caught -- meaning the CPU was almost certainly already running off the rails (matching the black-screen symptom) before ever reaching a meaningful `atari7800_pokey_step` call, not silently failing to write correctly. The static disassembly evidence (correct indirect store to a runtime-computed `0x0450+reg` address) stands independently and is now on more solid footing given this.
  - **Still not proven working end-to-end**, because the v3 bit can't be enabled on this platform's current 32KB ROM without breaking execution, and the C code has therefore never actually been runtime-tested against a real, responding POKEY device. Fixing this for real needs either a larger/bankswitched ROM option on this platform, or confirming the `a78_p450_t0` class actually works at 32KB (neither attempted). This is now a well-understood, scoped platform gap rather than a mystery.

## 15. 48K ROM + POKEY re-investigation (dead end, reverted); title screen layout/font fixes to match the reference exactly (Done 2026-09-06)

User re-raised POKEY after priority #14, pointing out the reference ROM is 48K non-bankswitched (astrowing.bas builds a flat 32K+16K image, not banked -- per [Bankset Bankswitching](https://7800.8bitdev.org/index.php/Bankset_Bankswitching) and the [A78 header spec](https://7800.8bitdev.org/index.php/A78_Header_Specification)) and asked for 48K + POKEY@$450 specifically for astrowing/title-demo, expecting the 32K-vs-48K mismatch found in priority #14 to be the actual blocker.

- **Implemented general 48K support in `link.ld`**: `__cart_rom_size` (32 or 48) drives `cart_rom`'s `ORIGIN = 0x10000 - size*1024`, matching the emulator's own `a78_rom_device::read_40xx`/`m_base_rom` formula exactly (confirmed by reading `a78_slot.cpp`/`rom.cpp` in this SDK's test emulator source). 48K alone (no POKEY) builds to the exact reference byte size (49280) and renders correctly.
- **48K did not fix the POKEY crash.** Rebuilding at 48K with the v3 POKEY bit set reproduced the identical full-black-screen failure as the earlier 32K attempt. Isolated via direct byte-patch A/B (clearing just the v3 bit on an already-built 48K+POKEY binary, no rebuild) that the crash is caused by the POKEY bit specifically, independent of ROM size -- disproving the priority #14 hypothesis.
- **Formed and disproved an IRQ-storm hypothesis.** Added `atari7800_pokey_init()` (writes 0 to IRQEN, since POKEY's IRQ output is wired to the 6502's IRQ line on this cart type and IRQEN's reset state isn't guaranteed clear) -- kept as a genuinely correct fix regardless, but didn't resolve the crash. Direct CPU status-flag tracing via Lua (`cpu.state["P"]`, checking the I bit) showed interrupts masked throughout the entire crash trace, ruling out a maskable IRQ storm; the repeated visits to the IRQ vector are a symptom of a stray `BRK` from garbage execution, not a hardware IRQ.
- **Given the scope of investigation already spent** (would need deep study of the emulator's `a78_p450_t0` C++ device internals, or real hardware, neither available), reverted the `--defsym` overrides in `examples/atari7800/CMakeLists.txt` back to 32K/no-POKEY for both `astrowing.a78` and `title-demo.a78`, restoring correct rendering (re-verified). Kept the underlying infrastructure (48K link.ld support, `atari7800_pokey_init`, POKEY register defines) since all of it is independently correct and will be needed whenever this is revisited. **POKEY audio remains unimplemented in this test emulator; still needs either real-hardware testing or deeper emulator-side study.**
- **`/Users/rowe/Software/rp6502/RPPacman/` (user's cited "definitive source" for assets) turned out to be unrelated** -- an rp6502/Picocomputer Pacman project, not Atari 7800, with no `unified_font.png` or Astrowing-relevant assets. The actual assets were found directly in the Astrowing source tree (`~/Software/Atari7800/Astrowing/src/graphics/`, `src/music/`) instead, which is what this and prior sections actually used.

Separately, user hands-on-tested `title-demo` again and gave five concrete gaps versus the reference: missing icon-glyph row, banner in the wrong position, "DIFFICULTY PRO" split across two lines instead of one, "VERSION 20260126" likewise, and (already known) no sound. All four graphical gaps fixed this session:

- **Found the real layout convention the reference uses**, resolving several long-standing guesses: 7800basic's `plotchars`/`plotvalue` "y" parameter is a *coarse line* (zone index), not a pixel coordinate -- confirmed from the 7800basic Guide's own wording ("the Y coordinates in character functions refer to a coarse/line position, not the screen Y coordinate") contrasted with `plotbanner`'s wording ("y is the y *screen* coordinate"). Re-deriving every title-screen element's exact zone from `astrowing.bas`'s actual calls (score y=0, icons y=1, DIFFICULTY/PRO both y=9, VERSION/date both y=11) showed they fit perfectly around a banner drawn at the real y=46 -- the y=64 approximation from priority #13/#14 was never necessary, just a workaround for not yet having non-zone-aligned banner support.
- **Non-zone-aligned banner positioning, implemented via per-zone row remap (no fine-Y/holey-DMA needed)**: added `--y-offset` to `utils/png_banner_to_zones_header.py`. For a banner at screen Y `y_offset`, the touched zones are `y_offset // zh` through `ceil((y_offset+height)/zh) - 1`; each remains a normal zone-aligned (shift=0) object, but zone Z's local row R now sources image row `(first_zone+Z)*zh + R - y_offset` (blank outside the image's own rows) instead of `Z*zh+R`. For the real title asset (96px tall at y=46, zh=16) this yields 7 touched zones (2-8) instead of 6 zone-aligned ones -- one more zone than a naive `height/zh`, because of the offset. This pushed total per-page bytes over MARIA's 256-byte page limit when packed the old way (all zones+chunks sharing one block): fixed by giving each chunk index its own shared page-block (one block per side-by-side chunk, all its zones sharing it) instead of one block for everything, since a single chunk's width is capped low enough (≤31 bytes) that even 7-8 zones of it comfortably fits one page. Emits `_FIRST_ZONE` so callers compute the real base pixel Y without re-deriving it.
- **Found and fixed a real bug in the hand-transcribed `hud_font` font asset** (`fighter.sprite.h`, dating to priority #13): its glyph pixel data was substantially wrong beyond just the untested icon glyphs -- a same-build A/B comparison (git-stashing this session's fix and re-screenshotting `astrowing.c`'s existing in-game HUD text) showed the *old* font already rendered most letters as broken/unreadable fragments, not just missing icons. Root-caused to the hand transcription, not the source asset. Fixed by writing a new, directly-auditable converter, `utils/png_indexed_font_to_strided_header.py`, that reads `unified_font.png` and emits the exact strided layout `atari7800_font_descriptor_t` actually uses (single shared `data` blob, glyph *i* at column `i*width_bytes` in every page, vertically flipped like every other `MARIA_STRIDED` asset) -- as opposed to the newer, unrelated `png_indexed_tileset_to_160a_font_header.py`/`astrowing_unified_font_160a.h` pairing discovered mid-session to be stale leftovers from an earlier, incompatible `atari7800_font_descriptor_t` shape (per-glyph `atari7800_sprite_asset_t` array) that was never actually wired into any example.
  - Also had to correctly determine the font's real tile height. `unified_font.png` is 17px tall; a naive top-8-rows crop (matching the *old* file's `glyph_height_lines=8`) renders letters/digits fine (their ink is entirely in the top 7 rows) but leaves the 5 icon glyphs (`*+-/<`, from `astrowing.bas`'s own `alphachars '...*+-/<>'` declaration, astrowing.bas:59 -- confirmed via the 7800basic compiler's own error strings, extracted from its wasm binary, showing `plotchars`/`alphadata` share one lookup table) almost entirely blank, since their real art extends down to row 15. Direct pixel inspection showed row 16 (the 17th) is blank across the whole strip -- a stray trailing row, not glyph content. Settled on `tile-height=16`, matching `zoneheight 16` exactly (astrowing.bas:6) -- the only value that both fits within one MARIA zone (an object's rendered height is fixed by its containing zone, not its own `height_lines` field, per priority #12's takeaway) and fully captures every icon glyph.
  - One pre-existing, non-regression artifact found and left as-is: `.`/`!`/`?`/`,`/`"`/`$`/`(`/`)`/`:`  (`astrowing.bas`'s declared but apparently never hand-drawn punctuation glyphs) render as a checkerboard placeholder box -- confirmed via direct pixel inspection that `unified_font.png` itself has a repeating gray/purple checkerboard at those exact tile positions, not real glyph art. This is an upstream asset gap (the artist never finished these glyphs), reproduced faithfully, not introduced by the new converter. Only affects `astrowing.c`'s in-game HUD ("SHLD:100"); `title-demo.c` never uses these characters.
- **`title-demo.c` rewritten to match the reference exactly**: real y=46 banner (now `TITLE_SCREEN_FIRST_ZONE`-based), palette 0 for the score (previously incorrectly used the font's default palette 5), a new icon-glyph row (`"*+-/<"`, palette 7, hue-cycling with the banner), and "DIFFICULTY"+"PRO" / "VERSION"+"20260126" merged back onto their real single zone rows each (needs `#define ATARI7800_SCENE_ZONE_BYTES 96` before `#include <atari7800.h>`, since 13 and 15 combined glyphs exceed the default 64-byte/12-glyph zone budget found in priority #14). Verified via side-by-side screenshot against the reference ROM at the same post-boot frame: matching score/icon/banner/DIFFICULTY-PRO/VERSION-date layout. Full regression pass (`palette-demo`, `displaylist-demo`, `asset-bridge-demo`, `astrowing`, `title-demo`) re-confirmed correct, including `astrowing.c`'s in-game HUD text now rendering correctly for the first time (previously silently broken by the same font bug this section fixed).
